#include "gpa.h"

char *gpconv(int a)
{
    int score;

    if (score >= 85)
    {
        return "A+";
    }
    else if (score >= 80)
    {
        return "A";
    }
    else (score >= 75);
    
    return "B+";
    
}